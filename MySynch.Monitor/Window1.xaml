<Window x:Class="MySynch.Monitor.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window1" Height="300" Width="300">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Grid.Row="1">
        <TreeView ScrollViewer.VerticalScrollBarVisibility="Auto"
                          BorderThickness="0"
                          Background="#FFF"
                          ItemContainerStyle="{DynamicResource TreeViewItemStyle}"
                          ItemsSource="{Binding NotificationDetailsCollection}"
                          ItemTemplate="{DynamicResource NotificationTemplate}" Visibility="{Binding TreeReady}">
        </TreeView>
        <Label Visibility="{Binding TreeNotReady}" Content="Loading tree ... please wait."/>
    </Grid>
</Window>
